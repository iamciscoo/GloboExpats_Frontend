# =============================================================================
# ENVIRONMENT VARIABLES TEMPLATE
# =============================================================================
# Copy this file to .env.local for development or .env.production for production
# Never commit actual .env files with real values to version control

# =============================================================================
# BACKEND API CONFIGURATION
# =============================================================================

# Backend API Base URL (internal, used by Next.js server)
# For production: use actual server IP/domain
# For local dev: use localhost or IP address
BACKEND_URL=http://10.123.22.21:8081

# Public API URL (used by browser clients)
# Base URL only - /api/v1 prefix is added in the API client
NEXT_PUBLIC_API_URL=http://10.123.22.21:8081

# Public Backend URL (full URL for direct backend access)
NEXT_PUBLIC_BACKEND_URL=http://10.123.22.21:8081

# WebSocket URL for real-time features
NEXT_PUBLIC_WS_URL=ws://10.123.22.21:8081/ws

# =============================================================================
# CDN & ASSETS
# =============================================================================

# CDN URL for static assets (optional)
# Leave empty to serve assets from Next.js server
NEXT_PUBLIC_CDN_URL=

# =============================================================================
# ENVIRONMENT SETTINGS
# =============================================================================

# Application environment (development, staging, production)
NODE_ENV=development
NEXT_PUBLIC_ENVIRONMENT=development

# Disable Next.js telemetry
NEXT_TELEMETRY_DISABLED=1

# =============================================================================
# SECURITY & CORS
# =============================================================================

# Allowed CORS origins (comma-separated, production only)
ALLOWED_ORIGINS=https://example.com,https://www.example.com

# =============================================================================
# DEPLOYMENT (Optional)
# =============================================================================

# Base path for deployment under a subpath (e.g., /frontend)
# NEXT_BASE_PATH=

# Asset prefix for CDN (must be absolute URL)
# ASSET_PREFIX=https://cdn.example.com

# =============================================================================
# FEATURE FLAGS (Optional)
# =============================================================================

# Enable/disable features
# NEXT_PUBLIC_ENABLE_ANALYTICS=true
# NEXT_PUBLIC_ENABLE_CHAT=true
# NEXT_PUBLIC_ENABLE_NOTIFICATIONS=true

# =============================================================================
# THIRD-PARTY SERVICES (Optional)
# =============================================================================

# Google OAuth (if implemented)
# NEXT_PUBLIC_GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=

# Analytics
# NEXT_PUBLIC_GA_ID=
# NEXT_PUBLIC_MIXPANEL_TOKEN=

# Error Tracking
# NEXT_PUBLIC_SENTRY_DSN=
# SENTRY_AUTH_TOKEN=

# =============================================================================
# NOTES
# =============================================================================
# 
# Environment Variable Naming:
# - NEXT_PUBLIC_* variables are exposed to the browser
# - Variables without NEXT_PUBLIC_ are only available server-side
# 
# Security Best Practices:
# - Never commit .env files with real values
# - Use different values for dev/staging/production
# - Rotate secrets regularly
# - Use environment-specific .env files (.env.local, .env.production)
# 
# Docker Deployment:
# - Pass environment variables via docker run -e or docker-compose
# - Or use build args for NEXT_PUBLIC_* variables
# - Example: docker run -e BACKEND_URL=http://backend:8000 ...
